name: Publish

# Manual only for now — trigger from the Actions tab.
on:
  workflow_dispatch:
    inputs:
      versionBump:
        description: "Version bump before publishing"
        type: choice
        default: none
        options:
          - none
          - patch
          - minor
          - major
      dryRun:
        description: "Package only, do not publish"
        type: boolean
        default: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      # See ci.yml: npm install (not ci) to tolerate rolldown's per-platform
      # optional native deps.
      - run: npm install --no-audit --no-fund

      # Full gate before anything is published.
      - run: npm run check-types
      - run: npm run lint
      - run: npm test

      - name: Bump version
        if: ${{ inputs.versionBump != 'none' }}
        run: npm version ${{ inputs.versionBump }} --no-git-tag-version

      - name: Package
        run: npx --yes @vscode/vsce package --no-dependencies -o revelo.vsix

      - name: Publish to VS Code Marketplace
        if: ${{ !inputs.dryRun }}
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: npx --yes @vscode/vsce publish --no-dependencies --packagePath revelo.vsix

      - name: Publish to Open VSX (Cursor)
        if: ${{ !inputs.dryRun }}
        run: npx --yes ovsx publish revelo.vsix -p ${{ secrets.OVSX_PAT }}

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v7
        with:
          name: revelo-vsix
          path: revelo.vsix
          if-no-files-found: error
